# T030.9 — Phase B Deferred (Real Redis Verification)

**Status**: Phase A **COMPLETE** (FINAL APPROVED). Phase B **DEFERRED** (ARCHITECT DECISION, Option 3 — APPROVED). T030.9 is **conditionally closed**: all structurally/unit/Phase-A-verifiable work is done and approved; real-Redis end-to-end verification is outstanding and explicitly deferred to a future, separately-authorized package (§6).

This document is the single, durable record of *why* Phase B could not be completed in this package, and exactly what still needs to happen before it can be. It exists so this gap is never silently forgotten or silently assumed closed.

---

## 1. Current Status

| Item | Status |
|---|---|
| T030.9 Phase A (bounded Redis client/BullMQ behavior, canonical config, health timeout, shutdown idempotency — all proven without a live Redis) | **COMPLETE**, FINAL APPROVED |
| T030.9 Phase B (real-Redis end-to-end proof) | **DEFERRED** |
| T030.9 overall | **Conditionally closed** — see §7 for release-readiness classification |

---

## 2. Why Local Verification Is Unavailable

Re-confirmed empirically in this sandbox (not assumed from memory):

- `which docker` → not found on `PATH`.
- `which redis-server` → not found on `PATH`.
- `which redis-cli` → not found on `PATH`.
- `netstat -ano | grep ":6379"` → nothing listening.
- A raw TCP connect to `REDIS_HOST:REDIS_PORT` (`localhost:6379`, per `backend/.env`) → `ECONNREFUSED`.
- No `REDIS_*` environment variable in this shell points at any remote instance, and none was supplied by the user for this package.

There is no local path to a real Redis instance in this environment.

---

## 3. Why GitHub CI Verification Is Unavailable

GitHub Actions run history **is** reachable (public REST API, unauthenticated, no credentials needed or printed), but provides no usable evidence for this package:

- **Latest successful run**: run #36, branch `main`, commit `a5eb72c` (this is also the current local `HEAD`).
- That run, and the one immediately before it (run #35, branch `feature/T014-sales-return`, commit `65c6ea8`), each executed **exactly one job** — `ci` ("Lint · Typecheck · Test · Prisma Validate · Build"), confirmed via `GET /repos/.../actions/runs/{id}/jobs` returning `"total_count": 1` both times.
- **No `e2e` job ran. No `docker-verify` job ran.** Neither appears in the job list at all (not even as "skipped").
- The `ci` job that did run uses dummy `DATABASE_URL`/`REDIS_HOST` values and **no service containers** — it never connects to a real Postgres or Redis.
- Commit `a5eb72c` (the latest pushed/run commit) **predates all of T030.6, T030.7, T030.8, and T030.9** — none of this package's code, nor any of the three packages before it, has ever been executed by GitHub Actions.

---

## 4. Local-vs-Pushed Workflow Difference

`git diff HEAD -- .github/workflows/backend-ci.yml` shows the `e2e` and `docker-verify` jobs exist **only as uncommitted local additions** — they are pure additions relative to `HEAD`, added to the local working tree during earlier T030.5/T030.7 work but never committed:

- **Local `backend-ci.yml`** (uncommitted working tree): 3 jobs — `ci`, `e2e` (real Postgres + Redis service containers, runs the full `*.e2e-spec.ts` suite), `docker-verify` (full Docker Compose deployment + bring-up verification).
- **Pushed `backend-ci.yml`** (current GitHub `HEAD`, commit `a5eb72c`): 1 job — `ci` only.

**Therefore: no CI evidence can be honestly claimed for the current code.** The workflow capable of proving Phase B exists only on this machine and has never run.

---

## 5. Phase B Acceptance Criteria — Still Outstanding

All of the following remain unproven against a real Redis instance:

- Real Redis service healthy and reachable.
- `GET /health` returns HTTP 200 (Redis available).
- Cart E2E (`cart.e2e-spec.ts`) passes.
- Checkout E2E (`checkout.e2e-spec.ts`) passes.
- Sales Return E2E (`sales-return.e2e-spec.ts`) passes.
- Invoice E2E (`invoice.e2e-spec.ts`) passes.
- Payment E2E (`payment.e2e-spec.ts`) passes.
- OTP/Auth Redis-dependent coverage passes.
- BullMQ (mail queue) initializes against a real Redis connection.
- Graceful shutdown closes the general Redis client and BullMQ Queue/Worker resources cleanly against a real connection.
- No Redis reconnect timer or open handle prevents Jest/Node from exiting.
- Full E2E suite (`npm run test:e2e`, unfiltered) passes with `--runInBand`.

---

## 6. Future Package Required

**Proposed title: T030.12 — CI Publication and Real Redis Verification.**

This future package requires its own, separate Architect Authorization — explicitly covering, at minimum:

- Commit scope (which files/changes are included).
- Branch (do not assume direct push to `main`).
- Commit message(s)/convention.
- Push target (fork, branch, or otherwise).
- Pull request strategy (if any).
- How the resulting CI run is to be observed/reported.
- How Phase B evidence is to be collected and presented once a real `e2e`/`docker-verify` run exists.

No assumption is made here about any of the above — they are all open questions for that future authorization to resolve.

---

## 7. Release-Readiness Impact

| Component | Classification |
|---|---|
| Backend code (T030.6–T030.9, all packages) | **READY** |
| Phase A Redis/BullMQ runtime behavior (bounded timeouts, canonical config, health degradation, shutdown idempotency) | **READY** |
| Real-Redis Phase B (Cart/Checkout/OTP/BullMQ/shutdown against a live Redis) | **DEFERRED** |
| Production release | **CONDITIONALLY READY** — release is not blocked on backend code quality, but must not be considered fully verified until Phase B passes |

---

## 8. No Implementation Statement

- No source code was changed to produce this document.
- No test file was changed.
- No CI/workflow file was changed.
- No `git commit` or `git push` was executed.

This package is documentation-only: it creates exactly this one file.
