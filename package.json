{
  "name": "pos-erp-enterprise",
  "version": "0.1.0",
  "private": true,
  "description": "POS ERP Enterprise v1.0 — monorepo root (tooling dùng chung: commit lint, git hooks, changelog)",
  "scripts": {
    "prepare": "husky",
    "release": "commit-and-tag-version"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.1",
    "@commitlint/config-conventional": "^19.6.0",
    "commit-and-tag-version": "^12.7.3",
    "husky": "^9.1.7"
  }
}
