# compiled output
/dist
/node_modules

# logs
logs
*.log
npm-debug.log*

# env
.env
.env.local
!.env.example

# IDE
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea

# coverage
/coverage

# prisma
prisma/*.db

# T051.03 — artifact backup PostgreSQL (pg_dump), KHÔNG BAO GIỜ commit lên git (chứa toàn bộ dữ
# liệu nghiệp vụ). Xem docs/release/BACKUP-RESTORE-RUNBOOK.md.
/backups/
*.dump

*.tsbuildinfo
